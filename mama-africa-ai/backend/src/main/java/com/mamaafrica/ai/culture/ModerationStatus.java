package com.mamaafrica.ai.culture;

/** Where a visitor submission sits in moderation. Only APPROVED is ever shown publicly. */
public enum ModerationStatus {
    PENDING,
    APPROVED,
    REJECTED
}
